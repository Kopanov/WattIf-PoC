# Runtime deps for the hosted Streamlit demo (Replit / Hugging Face Spaces).
# The app runs from ./src via the root streamlit_app.py shim; no package install needed.
streamlit>=1.33
plotly>=5.20
pandas>=2.0
numpy>=1.24
requests>=2.31
rdflib>=7.0
python-dotenv>=1.0
huggingface_hub>=0.24
pvlib>=0.10
